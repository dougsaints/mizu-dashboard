// HojePage — Phase 14-01.
// Wrapper temporário: renderiza o Dashboard atual completo.
// Plan 14-02 vai trocar pra um "Hoje" focado (alertas + recap + tendências do dia).

import Dashboard from './Dashboard'

export default function HojePage() {
  return <Dashboard />
}
