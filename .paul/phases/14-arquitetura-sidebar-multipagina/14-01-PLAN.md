---
phase: 14-arquitetura-sidebar-multipagina
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Sidebar.tsx
  - src/components/Layout.tsx
  - src/pages/HojePage.tsx
  - src/pages/RecapSemanalPage.tsx
  - src/pages/VendasPage.tsx
  - src/pages/ProdutosPage.tsx
  - src/pages/MarketingPage.tsx
  - src/pages/PadroesPage.tsx
  - src/pages/DiarioPage.tsx
  - src/pages/DadosPage.tsx
  - src/pages/Dashboard.tsx
  - src/App.tsx
  - src/index.css
autonomous: true
---

<objective>

## Goal

Criar o **shell de layout multi-página** do Sushi Mizú Dashboard: sidebar esquerda fixa com 7 navegações agrupadas em 3 categorias + React Router com 8 rotas + `/hoje` mostrando o painel atual completo. As outras 7 rotas mostram placeholder até a Phase 14-02 redistribuir o conteúdo.

## Purpose

Migrar de scroll-único pra multi-página é a **decisão arquitetural fundadora da v0.3**. Padrão validado em Stripe, Linear, Vercel, Plausible: landing "Hoje" responde a pergunta diária do Doug em zero cliques, sidebar dá endereço (URL) pra cada análise, e separa olhar (passivo) de fazer (ativo). Tudo depois disso pluga nessa estrutura.

## Output

Sidebar funcional + Layout shell + 8 rotas. Dashboard atual continua acessível em `/hoje` sem perder nada. Outras 7 rotas mostram placeholder pedagógico explicando que vão receber conteúdo no próximo plan.

</objective>

<context>

## Project Context

@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@.paul/phases/14-arquitetura-sidebar-multipagina/PHASE-OVERVIEW.md

## Source Files

@src/App.tsx
@src/pages/Dashboard.tsx
@src/components/Header.tsx
@src/lib/period.ts

## Decisões arquiteturais relevantes (do STATE.md)

- Sidebar esquerda 240px / 64px colapsada / drawer mobile (14-03 detalha mobile)
- Landing `/hoje` (não `/dashboard` ou `/`) — Doug abre 0-cliques na pergunta diária
- Agrupamento Visão Geral / Análises / Operação — separa olhar de fazer
- Sem atalhos teclado (Doug não usa)
- SVG inline lucide-style, zero nova dep

</context>

<acceptance_criteria>

## AC-1: Sidebar renderiza com estrutura correta

```gherkin
Given o usuário está autenticado e acessou qualquer rota do app
When o app monta em desktop (≥1024px)
Then a sidebar aparece à esquerda com:
  - Logo "水 Sushi Mizú" no topo
  - 3 headers de grupo uppercase (VISÃO GERAL, ANÁLISES, OPERAÇÃO)
  - 7 NavLinks com ícone SVG + label (Hoje, Recap Semanal, Vendas, Produtos, Marketing, Padrões, Diário, Dados)
  - Footer com avatar/email do usuário + botão logout
```

## AC-2: Toggle de colapso funciona e persiste

```gherkin
Given a sidebar está expandida (240px) no estado inicial
When o usuário clica no botão de colapso
Then a sidebar contrai pra 64px mostrando só os ícones
And o estado é salvo em localStorage como "mizu-sidebar-collapsed: true"
And ao recarregar a página, a sidebar permanece colapsada
And no estado colapsado, labels aparecem como tooltip nativo (atributo title)
```

## AC-3: 8 rotas funcionam com NavLink ativo destacado

```gherkin
Given o app está rodando
When o usuário navega entre rotas
Then todas as 8 funcionam:
  - /hoje, /recap-semanal, /vendas, /produtos, /marketing, /padroes, /diario, /dados
And / redireciona pra /hoje
And qualquer rota inválida (* catch-all) redireciona pra /hoje
And o NavLink da rota atual tem fundo gold/10% + border-left 3px gold
```

## AC-4: /hoje mostra o painel atual completo, outras 7 mostram placeholder

```gherkin
Given o usuário visita /hoje
Then ele vê exatamente o conteúdo que via antes em / (todas as sections do Dashboard atual)
And o painel funciona como antes (filtros, sync, gráficos, alertas)

Given o usuário visita /vendas (ou qualquer das 6 outras placeholder)
Then vê um placeholder pedagógico:
  - Título da página
  - Mensagem "Esta página será populada no plan 14-02 com [seções relevantes]"
```

## AC-5: Build limpo + HMR funciona

```gherkin
Given as mudanças foram feitas
When rodar `npm run build`
Then build completa sem erros, sem warnings novos
And `npm run dev` serve em http://localhost:5173 sem erros no console
And ao editar a Sidebar, HMR atualiza sem reload manual
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Criar Sidebar.tsx + CSS</name>
  <files>src/components/Sidebar.tsx, src/index.css</files>
  <action>
    Criar `src/components/Sidebar.tsx`:

    - Componente funcional React + TypeScript
    - Importa `NavLink` de react-router-dom
    - Importa `useAuth` de `../api/useAuth` pra obter user.email
    - Estado local `collapsed: boolean` inicializado de `localStorage.getItem('mizu-sidebar-collapsed') === 'true'`
    - Toggle salva em localStorage e atualiza estado
    - Estrutura JSX:
      - `<aside className={`sidebar ${collapsed ? 'sidebar-collapsed' : ''}`}>`
      - Header: logo 水 + label "Sushi Mizú" (oculto se colapsado) + botão chevron pra toggle (SVG inline)
      - 3 sections agrupadas por header uppercase pequeno (oculto se colapsado, mostra divider sutil):
        - VISÃO GERAL: Hoje, Recap Semanal
        - ANÁLISES: Vendas, Produtos, Marketing, Padrões
        - OPERAÇÃO: Diário, Dados
      - Footer: avatar circular (iniciais do email) + email truncado (oculto colapsado) + botão logout (SVG inline log-out, signOut do useAuth)
    - Cada NavLink:
      - `to={path}`, className callback retorna 'sidebar-link active' se isActive
      - Children: SVG icon (24×24, stroke 2, lucide-style) + span com label
      - Atributo `title={label}` pra tooltip nativo no estado colapsado
    - Ícones SVG inline lucide-style (path data):
      - Hoje: house (M3 12 L12 3 L21 12 ... + door)
      - Recap Semanal: calendar-check
      - Vendas: trending-up
      - Produtos: package
      - Marketing: megaphone
      - Padrões: activity (linha com bumps)
      - Diário: book-open
      - Dados: table-2 (ou database)
      - Chevron-left (toggle): chevron-left
      - Log-out: log-out (igual ao do Header)

    Adicionar ao fim de `src/index.css`:

    - `.sidebar`: position fixed, top 0, left 0, width 240px, height 100vh, background var(--ink-soft), border-right 1px solid var(--ink-border), display flex flex-direction column, padding 16px 0, transition width 200ms ease, z-index 100
    - `.sidebar-collapsed`: width 64px
    - `.sidebar-header`: padding 0 18px 16px, display flex align-items center justify-content space-between, border-bottom 1px solid var(--ink-border-subtle), margin-bottom 16px
    - `.sidebar-toggle`: button reset, padding 4px, background transparent, cursor pointer, color var(--txt-2), hover color var(--mizu-gold)
    - `.sidebar-group`: padding 8px 0, display flex flex-direction column gap 2px
    - `.sidebar-group-header`: padding 4px 18px 8px, font-size 10px, font-weight 800, text-transform uppercase, letter-spacing 1.5px, color var(--txt-3)
    - `.sidebar-collapsed .sidebar-group-header`: opacity 0, height 0, padding 0, overflow hidden (substituído por divider)
    - `.sidebar-link`: display flex align-items center gap 12px, padding 10px 18px, color var(--txt-1), font-size 14px, font-weight 500, text-decoration none, border-left 3px solid transparent, transition all 150ms
    - `.sidebar-link:hover`: background var(--ink-hover, rgba(255,255,255,0.04)), color var(--mizu-gold)
    - `.sidebar-link.active`: background rgba(201,169,97,0.10), border-left-color var(--mizu-gold), color var(--mizu-gold), font-weight 600
    - `.sidebar-collapsed .sidebar-link`: justify-content center, padding 10px 0
    - `.sidebar-collapsed .sidebar-link span`: display none
    - `.sidebar-link svg`: width 20px, height 20px, flex-shrink 0
    - `.sidebar-footer`: margin-top auto, padding 12px 18px, border-top 1px solid var(--ink-border-subtle), display flex align-items center gap 10px
    - `.sidebar-avatar`: width 32px, height 32px, border-radius 50%, background var(--mizu-gold), color var(--ink), display flex align-items center justify-content center, font-weight 700, font-size 12px, flex-shrink 0
    - `.sidebar-user-email`: font-size 12px, color var(--txt-2), overflow hidden, text-overflow ellipsis, white-space nowrap, flex-grow 1
    - `.sidebar-collapsed .sidebar-user-email, .sidebar-collapsed .sidebar-logo-label`: display none
    - `.sidebar-collapsed .sidebar-footer`: justify-content center, padding 12px 0
    - `@media (max-width: 1023px)`: `.sidebar { display: none }` (mobile drawer entra na 14-03)

    Avoid:
    - Importar lucide-react (zero dep nova; SVG inline)
    - Hardcodar cores fora dos CSS vars existentes
    - Usar emojis nos ícones (Doug já odeia visual WhatsApp)
  </action>
  <verify>npm run dev e abrir http://localhost:5173/hoje (após task 3) — sidebar aparece à esquerda com 7 NavLinks agrupados, toggle funciona, estado persiste após reload</verify>
  <done>AC-1, AC-2 satisfeitos: Sidebar renderiza com estrutura correta e toggle/persistência funcionam</done>
</task>

<task type="auto">
  <name>Task 2: Criar Layout.tsx shell + lift FilterProvider/Header pra fora do Dashboard</name>
  <files>src/components/Layout.tsx, src/pages/Dashboard.tsx</files>
  <action>
    Criar `src/components/Layout.tsx`:

    ```tsx
    import { Outlet } from 'react-router-dom'
    import Header from './Header'
    import Sidebar from './Sidebar'
    import { useAutoPollSales } from '../api/useSales'

    export default function Layout() {
      useAutoPollSales(300)  // mantém o polling de planilhas ativo globalmente
      return (
        <div className="app-shell">
          <Sidebar />
          <div className="app-shell-main">
            <Header />
            <main className="app-shell-content">
              <Outlet />
            </main>
          </div>
        </div>
      )
    }
    ```

    Adicionar ao `src/index.css`:
    - `.app-shell`: display flex, min-height 100vh, background var(--ink)
    - `.app-shell-main`: flex-grow 1, margin-left 240px, display flex flex-direction column, transition margin-left 200ms ease, min-width 0 (evita overflow)
    - `body:has(.sidebar-collapsed) .app-shell-main`: margin-left 64px
    - `@media (max-width: 1023px) { .app-shell-main { margin-left: 0 } }` (drawer em 14-03)
    - `.app-shell-content`: flex-grow 1, padding 24px 28px, max-width 1440px, margin 0 auto, width 100%

    Modificar `src/pages/Dashboard.tsx`:

    - **Remover** imports: `Header`, `useAutoPollSales`, `FilterProvider`
    - **Remover** `<Header />` do return
    - **Remover** wrapper `<FilterProvider>...</FilterProvider>`
    - **Remover** call `useAutoPollSales(300)` (agora vive no Layout)
    - **Remover** o `<main style={...}>` wrapper (Layout já provê `.app-shell-content`)
    - Manter apenas o conteúdo interno: OperationalAlerts + WeeklyRecap + sections em Suspense + uploads `<details>`
    - Wrap o conteúdo em `<>...</>` fragment

    Avoid:
    - Quebrar FilterProvider (vai pra App.tsx na task 3, NÃO sumir)
    - Esquecer de remover o `<Header />` (vai duplicar com o do Layout)
    - Mexer nas Suspense ou nos imports de sections (intacto pra 14-02 redistribuir)
  </action>
  <verify>npx tsc --noEmit — type-check passa sem erro</verify>
  <done>Pré-requisito pra AC-4: Layout existe e Dashboard.tsx ficou só com conteúdo (sem Header próprio)</done>
</task>

<task type="auto">
  <name>Task 3: Criar 8 page wrappers + atualizar App.tsx com rotas</name>
  <files>src/pages/HojePage.tsx, src/pages/RecapSemanalPage.tsx, src/pages/VendasPage.tsx, src/pages/ProdutosPage.tsx, src/pages/MarketingPage.tsx, src/pages/PadroesPage.tsx, src/pages/DiarioPage.tsx, src/pages/DadosPage.tsx, src/App.tsx, src/index.css</files>
  <action>
    Criar `src/pages/HojePage.tsx`:

    ```tsx
    import Dashboard from './Dashboard'
    export default function HojePage() {
      return <Dashboard />
    }
    ```

    Criar as 7 outras pages como placeholders pedagógicos (todos seguem o mesmo padrão):

    ```tsx
    // src/pages/VendasPage.tsx
    export default function VendasPage() {
      return (
        <div className="page-placeholder">
          <h1 className="page-placeholder-title">Vendas</h1>
          <p className="page-placeholder-text">
            Esta página vai receber: <strong>SalesSection, AnalysisSection, DataTableSection</strong>
            <br />Plan <code>14-02</code> redistribui o conteúdo.
          </p>
        </div>
      )
    }
    ```

    Mapping (título + sections que receberão) pra cada placeholder:

    - RecapSemanalPage: "Recap Semanal" — receberá `WeeklyRecap`
    - VendasPage: "Vendas" — receberá `SalesSection, AnalysisSection, DataTableSection`
    - ProdutosPage: "Produtos" — receberá `ProductsAnalysisSection`
    - MarketingPage: "Marketing" — receberá `MetaAdsAnalysisSection, RoiSection, MarketingUnif, CorrelationSection`
    - PadroesPage: "Padrões" — receberá `PatternsSection, TrendsSection`
    - DiarioPage: "Diário" — receberá `DiarioSection`
    - DadosPage: "Dados" — receberá `DataTableSection (export) + uploads <details>`

    Adicionar ao `src/index.css`:
    - `.page-placeholder`: padding 48px 24px, text-align center, color var(--txt-2)
    - `.page-placeholder-title`: font-size 28px, font-weight 700, color var(--mizu-gold), margin 0 0 16px
    - `.page-placeholder-text`: font-size 14px, line-height 1.6, max-width 480px, margin 0 auto

    Substituir `src/App.tsx` por:

    ```tsx
    import { lazy, Suspense } from 'react'
    import { Routes, Route, Navigate } from 'react-router-dom'
    import { useAuth } from './api/useAuth'
    import { FilterProvider } from './lib/period'
    import Login from './pages/Login'
    import Layout from './components/Layout'

    const HojePage = lazy(() => import('./pages/HojePage'))
    const RecapSemanalPage = lazy(() => import('./pages/RecapSemanalPage'))
    const VendasPage = lazy(() => import('./pages/VendasPage'))
    const ProdutosPage = lazy(() => import('./pages/ProdutosPage'))
    const MarketingPage = lazy(() => import('./pages/MarketingPage'))
    const PadroesPage = lazy(() => import('./pages/PadroesPage'))
    const DiarioPage = lazy(() => import('./pages/DiarioPage'))
    const DadosPage = lazy(() => import('./pages/DadosPage'))

    function AuthSplash() {
      return (
        <div className="auth-splash">
          <span className="kanji-mizu auth-splash-kanji">水</span>
        </div>
      )
    }

    export default function App() {
      const { user, isLoading } = useAuth()
      if (isLoading) return <AuthSplash />
      if (!user) return <Login />

      return (
        <FilterProvider>
          <Suspense fallback={<AuthSplash />}>
            <Routes>
              <Route element={<Layout />}>
                <Route path="/" element={<Navigate to="/hoje" replace />} />
                <Route path="/hoje" element={<HojePage />} />
                <Route path="/recap-semanal" element={<RecapSemanalPage />} />
                <Route path="/vendas" element={<VendasPage />} />
                <Route path="/produtos" element={<ProdutosPage />} />
                <Route path="/marketing" element={<MarketingPage />} />
                <Route path="/padroes" element={<PadroesPage />} />
                <Route path="/diario" element={<DiarioPage />} />
                <Route path="/dados" element={<DadosPage />} />
                <Route path="*" element={<Navigate to="/hoje" replace />} />
              </Route>
            </Routes>
          </Suspense>
        </FilterProvider>
      )
    }
    ```

    Notas:
    - `FilterProvider` agora vive acima de Routes pra persistir filtros entre páginas
    - `<Layout />` como pai (com `<Outlet />`) e todas as 8 pages como filhos
    - Catch-all `*` redireciona pra /hoje
    - Pages todas lazy-loaded (mesmo padrão de Dashboard anterior)

    Avoid:
    - Esquecer de wrap em FilterProvider (filtros vão sumir entre páginas)
    - Deixar a rota antiga `/ → Dashboard` (precisa virar `/ → Navigate /hoje`)
    - Page placeholder usar emoji (idem visual WhatsApp)
  </action>
  <verify>
    1. npm run build — sem erros
    2. npm run dev — abre em http://localhost:5173, redireciona automaticamente pra /hoje
    3. Sidebar visível à esquerda em desktop, /hoje mostra todo o conteúdo do dashboard anterior
    4. Clicar em Vendas → URL muda pra /vendas, conteúdo vira placeholder "Esta página vai receber..."
    5. Voltar em Hoje, abrir uploads, mudar período no header — filtro persiste se navegar pra outra página (FilterProvider acima de Routes)
    6. Toggle de sidebar colapsa, reload mantém colapsado
  </verify>
  <done>AC-3, AC-4, AC-5 satisfeitos: 8 rotas funcionam, /hoje preserva painel atual, outras mostram placeholder, build limpo</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE

- `src/sections/*` — Phase 14-02 redistribui sections em pages; 14-01 não toca nelas
- `src/components/OperationalAlerts.tsx` — Phase 15 transforma em banner persistente; 14-01 mantém dentro do Dashboard
- `src/components/Header.tsx` — alinhamento header/subheader vai na Phase 14-04
- `src/api/*` — hooks intactos, FilterProvider só mudou de localização
- `src/lib/period.ts` (FilterProvider) — só importação muda, lógica intacta
- `supabase/migrations/*` — sem mudança de schema na v0.3

## SCOPE LIMITS

- **Mobile drawer não entra nessa task** — 14-03 cuida; 14-01 só esconde sidebar abaixo de 1023px (`display: none`)
- **Redistribuir sections em pages NÃO entra** — 14-02 faz isso; pages 7/8 são placeholder
- **Alinhamento header/subheader NÃO entra** — 14-04 faz; Header continua como está
- **Banner persistente de alerta NÃO entra** — Phase 15
- Não adicionar nova dependência (sem lucide-react, sem heroicons, sem nada — SVG inline)
- Não tocar em CSS existente que não seja relacionado a sidebar/layout/page-placeholder (mantém v0.2 intacto)

</boundaries>

<verification>

Before declaring plan complete:

- [ ] `npx tsc --noEmit` passa sem erros
- [ ] `npm run build` completa sem warnings novos
- [ ] `npm run dev` serve sem erros no console
- [ ] http://localhost:5173/ redireciona pra /hoje
- [ ] /hoje mostra dashboard completo (todas as sections + uploads + alertas)
- [ ] Sidebar visível em desktop ≥1024px com 7 NavLinks agrupados em 3 categorias
- [ ] Toggle de colapso funciona (240 ↔ 64px)
- [ ] localStorage `mizu-sidebar-collapsed` persiste estado após reload
- [ ] NavLink ativo destacado em gold com border-left
- [ ] /vendas, /produtos, /marketing, /padroes, /diario, /dados mostram placeholder
- [ ] /recap-semanal mostra placeholder
- [ ] /qualquer-coisa-invalida redireciona pra /hoje
- [ ] Filtros do Header persistem ao navegar entre páginas (FilterProvider acima de Routes)
- [ ] Sidebar oculta abaixo de 1024px (mobile = drawer entra em 14-03)
- [ ] AC-1, AC-2, AC-3, AC-4, AC-5 todos satisfeitos

</verification>

<success_criteria>

- All 3 tasks completed
- Build limpo (sem warnings novos)
- Sidebar funcional em desktop com toggle persistente
- 8 rotas funcionando com NavLink ativo destacado
- /hoje preserva 100% do dashboard atual (zero regressão)
- 7 outras pages mostram placeholder pedagógico
- FilterProvider lifted pra acima de Routes (filtros persistem entre páginas)

</success_criteria>

<output>

After completion, create `.paul/phases/14-arquitetura-sidebar-multipagina/14-01-SUMMARY.md` com:

- Goal achieved
- Files criados/modificados
- Decisões tomadas (SVG inline vs lucide-react, FilterProvider lifted, etc)
- Screenshots/observações pra UAT
- Próximo plan: 14-02 redistribui sections em pages
- Deferred: mobile drawer (14-03), alinhamento header (14-04)

</output>
