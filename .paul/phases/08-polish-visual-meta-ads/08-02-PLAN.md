---
phase: 08-polish-visual-meta-ads
plan: 02
type: execute
autonomous: true
---

<objective>
## Goal
Portar 3 visualizações de Meta Ads do painel HTML pro app React: **distribuição da verba por categoria de campanha** (Performance/Engajamento/Alcance), **distribuição da verba por unidade** (Jatiúca/Serraria/Geral) e **hero card "Visibilidade Jatiúca"** com métricas isoladas da unidade.

Doug pediu explicitamente esses 3 itens.

## Purpose
- Mostra **onde o dinheiro de Meta Ads tá indo** — saber se está distribuído nos objetivos certos
- Hero Jatiúca: destaca a unidade que mais precisa de visibilidade (Jatiúca = praia, conteúdo turístico)

## Output
- `src/lib/adsCategory.ts` — funções puras: `classifyCampaign(name)`, `unitOfCampaign(name)`, `ADS_GOAL_GROUPS`
- `src/components/AdsCategoryDonut.tsx` — donut da distribuição da verba por categoria de objetivo
- `src/components/AdsUnitDonut.tsx` — donut da distribuição da verba por unidade
- `src/components/JatiucaVisibilityCard.tsx` — card hero azul claro com métricas de Jatiúca
- `src/sections/MetaAdsAnalysisSection.tsx` — orquestra os 3 (grid 2 cols + card hero embaixo)
- Wire-up no `Dashboard.tsx` (após ProductsAnalysisSection)
- CSS apendado
- `.paul/phases/08-polish-visual-meta-ads/08-02-SUMMARY.md`
</objective>

<context>
@.paul/PROJECT.md
@src/api/useAds.ts
@src/lib/period.tsx
@src/pages/Dashboard.tsx
@painel-diario_BKP_2026-05-16_v7-atual.html — linhas 2466-2489 (classifyCampaign, unitOfCampaign), 3846-3856 (ADS_GOAL_GROUPS), 919-925 (jat-hero CSS)
</context>

<acceptance_criteria>

## AC-1: Helpers puros adsCategory.ts

```gherkin
Given uma string de nome de campanha
When chamo classifyCampaign(name)
Then retorna 'rmk' | 'ven' | 'eng' | 'alc' | 'out' conforme regex do painel original (linhas 2473-2481)
  And normaliza string: uppercase + remove acentos via NFD antes de matchar
  And unitOfCampaign(name) retorna 'jatiuca' | 'serraria' | 'geral' por menção no nome
  And export ADS_GOAL_GROUPS = [{key, label, cats, color}] agrupando rmk+ven=Performance, eng=Engajamento, alc+out=Alcance/Branding
```

## AC-2: AdsCategoryDonut

```gherkin
Given rows de ads_daily no período
When AdsCategoryDonut renderiza
Then agrupa cost por ADS_GOAL_GROUPS (3 fatias: Performance, Engajamento, Alcance/Branding)
  And mostra donut com cor por goal (verde performance, azul engajamento, dourado alcance)
  And tooltip: " Performance: R$ X (Y%)"
  And total centralizado com R$ e label "investido"
  And empty state se sum=0
```

## AC-3: AdsUnitDonut

```gherkin
Given rows de ads_daily no período
When AdsUnitDonut renderiza
Then agrupa cost por unitOfCampaign(campaign_name) — 3 fatias: Jatiúca, Serraria, Geral
  And cores: Jatiúca azul, Serraria roxo (paleta consistente com SalesLineChart), Geral cinza
  And tooltip: " Jatiúca: R$ X (Y%)"
  And total centralizado com label "verba do período"
```

## AC-4: JatiucaVisibilityCard

```gherkin
Given rows de ads_daily filtradas em unitOfCampaign === 'jatiuca'
When JatiucaVisibilityCard renderiza
Then bloco com:
  - Header: 📍 "Visibilidade Jatiúca (Praia)" + subtítulo "Quanto a unidade está sendo mostrada em campanhas específicas"
  - 3-4 KPIs grandes lado a lado: Investimento, Alcance, Impressões, Cliques (% do total geral em badge sub)
  - Visual hero azul claro: gradient `#E8F2FA → #fff` (paleta praia/Jatiúca)
  - Se 0 rows de Jatiúca, mostra "Sem campanhas direcionadas pra Jatiúca no período"
```

## AC-5: MetaAdsAnalysisSection — orquestra

```gherkin
Given useFilters retorna {start, end}
When MetaAdsAnalysisSection renderiza
Then layout:
  - Header `.mizu-section` com kanji 広 (alcance/anúncios)
  - Grid 2 cols: AdsCategoryDonut + AdsUnitDonut
  - JatiucaVisibilityCard abaixo
  And busca useAds(start, end, { subscribeRealtime: false }) — AdsUploadCard mantém canal
  And NÃO aplica filtros unit/channel (decisão Phase 5: Meta Ads não filtra por unit/channel global)
  And renderiza após ProductsAnalysisSection
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: src/lib/adsCategory.ts</name>
  <files>src/lib/adsCategory.ts</files>
  <action>
    ```ts
    function normalize(s: string): string {
      return s.toUpperCase().normalize('NFD').replace(/[̀-ͯ]/g, '')
    }

    export type AdsCategory = 'rmk' | 'ven' | 'eng' | 'alc' | 'out'

    export function classifyCampaign(name: string): AdsCategory {
      const u = normalize(name)
      if (u.includes('[RMK]') || u.includes('REMARKET')) return 'rmk'
      if (u.includes('DELIVERY')) return 'ven'
      if (u.includes('[VEN]') || u.includes('[V2]') || u.includes('[VENDAS]')) return 'ven'
      if (u.includes('[ENG]') || u.includes('[ENGAJ')) return 'eng'
      if (u.includes('[ALC]') || u.includes('[ALCANCE]') || u.includes('BRANDING')) return 'alc'
      return 'out'
    }

    export type AdsUnit = 'jatiuca' | 'serraria' | 'geral'
    export function unitOfCampaign(name: string): AdsUnit {
      const u = normalize(name)
      if (u.includes('JATIUCA')) return 'jatiuca'
      if (u.includes('SERRARIA')) return 'serraria'
      return 'geral'
    }

    export type AdsGoalGroup = {
      key: 'perf' | 'eng' | 'alc'
      label: string
      cats: AdsCategory[]
      color: string
    }
    export const ADS_GOAL_GROUPS: AdsGoalGroup[] = [
      { key: 'perf', label: 'Performance',         cats: ['ven', 'rmk'], color: '#27AE60' },
      { key: 'eng',  label: 'Engajamento',         cats: ['eng'],         color: '#2980B9' },
      { key: 'alc',  label: 'Alcance / Branding',  cats: ['alc', 'out'],  color: '#C9A961' },
    ]
    ```
  </action>
  <verify>tsc passa; testes mentais: classifyCampaign('[RMK] Vendas serraria') === 'rmk'; unitOfCampaign('Branding Jatiúca') === 'jatiuca'</verify>
  <done>AC-1</done>
</task>

<task type="auto">
  <name>Task 2: AdsCategoryDonut.tsx</name>
  <files>src/components/AdsCategoryDonut.tsx</files>
  <action>Componente similar ao CategoryDonutChart (delivery) mas usa ADS_GOAL_GROUPS. Recebe `rows: AdsRow[]`, agrupa cost por classifyCampaign → goalGroup. Donut Chart.js com 3 fatias coloridas. Total no centro: R$ XK "investido". Empty state se total=0.</action>
  <verify>build passa</verify>
  <done>AC-2</done>
</task>

<task type="auto">
  <name>Task 3: AdsUnitDonut.tsx</name>
  <files>src/components/AdsUnitDonut.tsx</files>
  <action>Componente análogo: agrupa cost por unitOfCampaign. 3 fatias (Jatiúca azul #2980B9, Serraria roxo #8E44AD, Geral cinza #95A5A6). Total no centro.</action>
  <verify>build</verify>
  <done>AC-3</done>
</task>

<task type="auto">
  <name>Task 4: JatiucaVisibilityCard.tsx</name>
  <files>src/components/JatiucaVisibilityCard.tsx</files>
  <action>
    Card com header `.jat-hero-title` + 3-4 KPIs em grid:
    - Investimento Jatiúca (R$) + sub "% do total geral"
    - Alcance Jatiúca (número) + sub "% do total"
    - Impressões Jatiúca + sub
    - Cliques Jatiúca + sub
    Calcula via `rows.filter(r => unitOfCampaign(r.campaign_name) === 'jatiuca')` vs total geral.
    Visual: gradient `#E8F2FA → #fff`, border azul claro, sombra suave. Padding generoso.
    Empty state: "Sem campanhas direcionadas pra Jatiúca no período" se 0 rows.
  </action>
  <verify>build</verify>
  <done>AC-4</done>
</task>

<task type="auto">
  <name>Task 5: MetaAdsAnalysisSection.tsx + wire-up</name>
  <files>src/sections/MetaAdsAnalysisSection.tsx, src/pages/Dashboard.tsx, src/index.css</files>
  <action>
    1. Section orquestra os 3 components, useAds(start, end, {subscribeRealtime:false}).
       Layout:
       ```
       header .mizu-section-head (kanji 広)
       .ads-analysis-grid (2 cols): AdsCategoryDonut | AdsUnitDonut
       JatiucaVisibilityCard
       ```
    2. Dashboard.tsx: lazy() + Suspense, render após ProductsAnalysisSection
    3. CSS: .ads-analysis-grid (grid 2 cols → 1 mobile), .jat-hero (gradient azul claro, border-left azul forte), .jat-hero-title (font-weight 700, font-size 14), .jat-kpis (grid 4 cols → 2 mobile), .jat-kpi-* (label/value/sub)
  </action>
  <verify>build, novo chunk MetaAdsAnalysisSection-*.js</verify>
  <done>AC-5</done>
</task>

<task type="auto">
  <name>Task 6: build + push</name>
  <files>(git)</files>
  <action>npm run build, commit, push, Vercel auto-deploya</action>
  <done>deployed</done>
</task>

</tasks>

<output>
.paul/phases/08-polish-visual-meta-ads/08-02-SUMMARY.md
</output>
