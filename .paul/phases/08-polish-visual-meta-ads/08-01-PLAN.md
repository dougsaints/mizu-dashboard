---
phase: 08-polish-visual-meta-ads
plan: 01
type: execute
autonomous: true
---

<objective>
## Goal
Aplicar **KPI hero card** (fundo preto com gradient + kanji 水 watermark dourado + texto branco/dourado) nos KPIs estrela do painel — destacando visualmente o número mais importante de cada seção. Fiel ao painel HTML original (`.kpi-card.kpi-hero` linhas 393-415).

## Purpose
Doug deu feedback: "o painel HTML monolítico era mais bonito... cards pretos com fundo degrade bem estilizado". Fix.
</objective>

<context>
@.paul/PROJECT.md
@src/sections/SalesSection.tsx
@src/sections/TrendsSection.tsx
@src/index.css
@painel-diario_BKP_2026-05-16_v7-atual.html — linhas 393-415 (.kpi-hero CSS canônico)
</context>

<acceptance_criteria>
## AC-1: Classe .kpi-hero no CSS
Given index.css
When inspeciono
Then existe `.kpi-card.kpi-hero` com gradient escuro (var(--mizu-ink-2) → var(--mizu-ink))
  And border 1px var(--mizu-gold-deep)
  And box-shadow profunda + inset gold
  And kanji 水 watermark via ::before, opacity 0.08, posição canto inferior direito
  And textos kpi-label/kpi-value/kpi-delta com cores ajustadas pro fundo escuro

## AC-2: Hero aplicado no card Total Geral (SalesSection)
Given SalesSection renderiza KPIs
When o primeiro card "Total geral · período selecionado" aparece
Then tem classe combinada `kpi-card kpi-hero`
  And Ticket médio NÃO tem hero (só 1 hero por seção pra ter impacto)
  And mobile: hero não fica estranho (kanji watermark escalado proporcionalmente)

## AC-3: Hero aplicado no card Faturamento (TrendsSection)
Given TrendsSection grid de 6 cards
When o primeiro card "Faturamento" aparece
Then tem classe `trend-card trend-card--hero` (analogia ao kpi-hero mas adaptado pra .trend-card)
  And demais 5 trend-cards seguem visual atual
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: CSS .kpi-hero + .trend-card--hero</name>
  <files>src/index.css</files>
  <action>Apender no fim do CSS:
- `.kpi-card.kpi-hero` com gradient ink-2 → ink, border gold-deep, shadow profunda
- `.kpi-card.kpi-hero::before` com kanji 水 grande no canto, opacity 0.08
- `.kpi-card.kpi-hero .kpi-label` rgba(255,255,255,0.7); .kpi-value #fff; .kpi-sub rgba(.6); .kpi-delta variants no escuro
- `.trend-card--hero` mesma ideia mas com tamanhos/padding do .trend-card (menor que kpi-card)
- Mobile: kanji watermark menor</action>
  <verify>build passa</verify>
  <done>AC-1</done>
</task>

<task type="auto">
  <name>Task 2: SalesSection — hero no Total Geral</name>
  <files>src/sections/SalesSection.tsx</files>
  <action>Trocar `<div className="kpi-card">` do "Total geral" por `<div className="kpi-card kpi-hero">`. Manter Ticket médio normal.</action>
  <verify>visual: card preto com gold no Total</verify>
  <done>AC-2</done>
</task>

<task type="auto">
  <name>Task 3: TrendsSection — hero no Faturamento</name>
  <files>src/components/TrendCard.tsx, src/sections/TrendsSection.tsx</files>
  <action>Adicionar prop `hero?: boolean` no TrendCard; aplicar `trend-card--hero` se true. Passar `hero={true}` apenas no card key='faturamento' do array metrics da TrendsSection.</action>
  <verify>visual: 1º card escuro, demais claros</verify>
  <done>AC-3</done>
</task>

<task type="auto">
  <name>Task 4: Build + push</name>
  <files>(git)</files>
  <action>build + commit + push, Vercel auto-deploya</action>
  <verify>push OK, novo bundle no Vercel</verify>
  <done>deployed</done>
</task>
</tasks>

<output>
.paul/phases/08-polish-visual-meta-ads/08-01-SUMMARY.md
</output>
