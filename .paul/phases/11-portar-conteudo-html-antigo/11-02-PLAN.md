---
phase: 11-portar-conteudo-html-antigo
plan: 02
type: execute
wave: 1
depends_on: ["11-01"]
files_modified:
  - src/sections/MetaAdsAnalysisSection.tsx
  - src/index.css
autonomous: true
---

<objective>
## Goal
Complementar os donuts existentes do Meta Ads com **tabela por categoria** (Performance/Engajamento/Alcance) e **cards por unidade** (Jatiúca/Serraria/Geral) mostrando valores absolutos + percentual. Donut = proporção; tabela/cards = números nus.

## Output
- Tabela `.cat-breakdown-table` no chart-card de Categoria
- Cards `.unit-breakdown-cards` no chart-card de Unidade
- CSS pra ambos
</objective>

<context>
@src/sections/MetaAdsAnalysisSection.tsx
@src/lib/adsCategory.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Adicionar tabela categoria + cards unidade no MetaAdsAnalysisSection</name>
  <files>src/sections/MetaAdsAnalysisSection.tsx</files>
  <action>
    Importar `classifyCampaign`, `goalGroupOf`, `ADS_GOAL_GROUPS`, `unitOfCampaign` de `../lib/adsCategory`.

    Calcular via useMemo:
    - `byGoal: Record<AdsGoalKey, number>` — soma de cost por goal group
    - `byUnit: Record<AdsUnit, number>` — soma de cost por unit

    Dentro do chart-card de "Distribuição por Objetivo", APÓS o `<AdsCategoryDonut rows={rows} />`, adicionar:
    ```tsx
    <table className="cat-breakdown-table">
      <thead><tr><th>Categoria</th><th className="r">Valor</th><th className="r">% do total</th></tr></thead>
      <tbody>
        {ADS_GOAL_GROUPS.map(g => {
          const v = byGoal[g.key] ?? 0
          const pct = totals.cost > 0 ? (v / totals.cost) * 100 : 0
          return (
            <tr key={g.key}>
              <td><span className="cat-dot" style={{ background: g.color }}></span>{g.label}</td>
              <td className="r">{brl0(v)}</td>
              <td className="r">{pct.toFixed(1).replace('.', ',')}%</td>
            </tr>
          )
        })}
      </tbody>
    </table>
    ```

    Dentro do chart-card de "Distribuição por Unidade", APÓS o `<AdsUnitDonut rows={rows} />`, adicionar:
    ```tsx
    <div className="unit-breakdown-cards">
      {(['jatiuca','serraria','geral'] as const).map(u => {
        const v = byUnit[u] ?? 0
        const pct = totals.cost > 0 ? (v / totals.cost) * 100 : 0
        const label = u === 'jatiuca' ? 'Jatiúca' : u === 'serraria' ? 'Serraria' : 'Geral'
        return (
          <div key={u} className={`unit-breakdown-card unit-breakdown-card--${u}`}>
            <div className="unit-breakdown-lbl">{label}</div>
            <div className="unit-breakdown-val">{brl0(v)}</div>
            <div className="unit-breakdown-pct">{pct.toFixed(1).replace('.', ',')}%</div>
          </div>
        )
      })}
    </div>
    ```
  </action>
  <verify>grep cat-breakdown-table src/sections/MetaAdsAnalysisSection.tsx — 1+ matches</verify>
  <done>Tabela e cards renderizados</done>
</task>

<task type="auto">
  <name>Task 2: CSS</name>
  <files>src/index.css</files>
  <action>
    Appendar:
    ```css
    /* ============================================================
       Category breakdown table + Unit breakdown cards — Phase 11-02
    ============================================================ */
    .cat-breakdown-table {
      width: 100%;
      border-collapse: collapse;
      font-size: 12px;
      margin-top: 12px;
    }
    .cat-breakdown-table th {
      text-align: left;
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 0.6px;
      text-transform: uppercase;
      color: var(--txt-2);
      padding: 6px 8px;
      border-bottom: 1px solid var(--border);
    }
    .cat-breakdown-table th.r,
    .cat-breakdown-table td.r {
      text-align: right;
    }
    .cat-breakdown-table td {
      padding: 8px;
      border-bottom: 1px solid rgba(232,224,204,0.4);
      color: var(--txt);
    }
    .cat-breakdown-table tr:last-child td { border-bottom: none; }
    .cat-dot {
      display: inline-block;
      width: 9px; height: 9px;
      border-radius: 50%;
      margin-right: 8px;
      vertical-align: middle;
    }

    .unit-breakdown-cards {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 8px;
      margin-top: 12px;
    }
    .unit-breakdown-card {
      background: #fff;
      border: 1px solid var(--border);
      border-left: 3px solid var(--txt-3);
      border-radius: 8px;
      padding: 8px 10px;
    }
    .unit-breakdown-card--jatiuca  { border-left-color: var(--unit-jatiuca); }
    .unit-breakdown-card--serraria { border-left-color: var(--unit-serraria); }
    .unit-breakdown-card--geral    { border-left-color: var(--txt-3); }
    .unit-breakdown-lbl {
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 0.6px;
      text-transform: uppercase;
      color: var(--txt-2);
    }
    .unit-breakdown-card--jatiuca .unit-breakdown-lbl  { color: var(--unit-jatiuca); }
    .unit-breakdown-card--serraria .unit-breakdown-lbl { color: var(--unit-serraria); }
    .unit-breakdown-val {
      font-size: 17px;
      font-weight: 800;
      color: var(--txt);
      letter-spacing: -0.3px;
      margin-top: 2px;
    }
    .unit-breakdown-pct {
      font-size: 11px;
      color: var(--txt-3);
      font-weight: 600;
      margin-top: 2px;
    }
    ```
  </action>
  <verify>npx tsc --noEmit && npm run build</verify>
  <done>Build limpo</done>
</task>
</tasks>

<boundaries>
- NÃO mexer nos donuts existentes
- NÃO mexer no JatiucaVisibilityCard
- NÃO mexer no bloco meta-kpis-grid (11-01)
</boundaries>

<output>SUMMARY em 11-02-SUMMARY.md</output>
