---
phase: 13-exportacao-png-pdf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/exportPng.ts (new)
  - src/components/SectionHeader.tsx
  - src/index.css
autonomous: true
---

<objective>
## Goal
Botão "📷" no header de cada seção (default ligado) que exporta a seção como PNG via html2canvas. Pra envio rápido por WhatsApp.

## Output
- `src/lib/exportPng.ts` novo
- `SectionHeader.tsx` com prop `exportable?: boolean` (default true) renderizando botão
- CSS pro botão
</objective>

<context>
@src/components/SectionHeader.tsx
@package.json (html2canvas ^1.4.1 já instalado)
</context>

<tasks>
<task type="auto">
  <name>Task 1: Lib exportPng</name>
  <files>src/lib/exportPng.ts</files>
  <action>
    Criar com função:
    ```ts
    import html2canvas from 'html2canvas'

    export async function exportSectionAsPng(
      sectionEl: HTMLElement,
      filename: string,
    ): Promise<void> {
      const canvas = await html2canvas(sectionEl, {
        backgroundColor: '#FFFCF3',
        scale: 2,
        logging: false,
        useCORS: true,
      })
      const dataUrl = canvas.toDataURL('image/png')
      const link = document.createElement('a')
      link.href = dataUrl
      link.download = filename
      document.body.appendChild(link)
      link.click()
      document.body.removeChild(link)
    }

    export function safeFilename(title: string): string {
      const slug = title
        .toLowerCase()
        .normalize('NFD')
        .replace(/[̀-ͯ]/g, '')
        .replace(/[^a-z0-9]+/g, '-')
        .replace(/^-+|-+$/g, '')
      const date = new Date().toISOString().slice(0, 10)
      return `mizu-${slug}-${date}.png`
    }
    ```
  </action>
  <verify>file exists, tsc limpo</verify>
  <done>lib criada</done>
</task>

<task type="auto">
  <name>Task 2: SectionHeader com exportable prop</name>
  <files>src/components/SectionHeader.tsx</files>
  <action>
    Adicionar:
    - Import `useRef` from 'react'
    - Import `exportSectionAsPng, safeFilename` from '../lib/exportPng'
    - Prop `exportable?: boolean` (default true)
    - `ref` no wrapper `.section-header`
    - Botão "📷" no `.section-header-actions` ANTES de `actions` (e antes da source-badge se possível, ou depois)

    Quando clicado, busca o `.mizu-section` ancestral via `headerRef.current?.closest('.mizu-section')`, chama exportSectionAsPng com filename = safeFilename(title).

    Mostrar estado "exportando…" enquanto roda (useState).

    Não usa `any`. Lazy load html2canvas pra não engordar bundle inicial — usar dynamic import dentro do click handler:
    ```ts
    async function handleExport() {
      setBusy(true)
      try {
        const section = headerRef.current?.closest('.mizu-section') as HTMLElement | null
        if (!section) return
        const { exportSectionAsPng, safeFilename } = await import('../lib/exportPng')
        await exportSectionAsPng(section, safeFilename(title))
      } finally {
        setBusy(false)
      }
    }
    ```
  </action>
  <verify>tsc limpo, build limpo</verify>
  <done>botão renderiza, click exporta</done>
</task>

<task type="auto">
  <name>Task 3: CSS botão export</name>
  <files>src/index.css</files>
  <action>
    Appendar:
    ```css
    /* Export button — Phase 13-01 */
    .section-export-btn {
      background: var(--mizu-cream);
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 4px 10px;
      font-size: 14px;
      cursor: pointer;
      transition: background 0.15s, border-color 0.15s;
      line-height: 1;
      min-height: 30px;
    }
    .section-export-btn:hover {
      background: #F0EBD9;
      border-color: var(--mizu-gold);
    }
    .section-export-btn:disabled {
      opacity: 0.5;
      cursor: wait;
    }
    @media print {
      .section-export-btn { display: none !important; }
    }
    ```
  </action>
  <verify>build limpo</verify>
  <done>CSS aplicado</done>
</task>
</tasks>

<boundaries>
- Sem nova dependência (html2canvas já tem)
- Não mexer em sections (mudança encapsulada no SectionHeader)
</boundaries>

<output>13-01-SUMMARY.md</output>
