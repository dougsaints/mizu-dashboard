---
phase: 03-anotaai-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0004_anotaai_products.sql
  - src/lib/anotaaiCsv.ts
  - src/api/useAnotaai.ts
  - src/components/AnotaaiUploadCard.tsx
  - src/pages/Dashboard.tsx
  - src/types/database.ts
  - src/index.css
autonomous: false
---

<objective>
## Goal
Finalizar e commitar a seção Anota AI: rodar a migration no banco, verificar o componente de upload no browser, e fechar o trabalho em commit.

## Purpose
Todo o código desta fase já foi escrito. O único passo pendente é executar a migration no Supabase (que cria as tabelas `anotaai_imports` e `anotaai_products`) para que o componente de upload funcione na prática.

## Output
- Tabelas `anotaai_imports` e `anotaai_products` criadas no Supabase
- Componente `AnotaaiUploadCard` funcionando (upload de CSV, lista de imports)
- Todos os arquivos commitados no git
</objective>

<context>
@.paul/PROJECT.md
@supabase/migrations/0004_anotaai_products.sql
@src/components/AnotaaiUploadCard.tsx
@src/api/useAnotaai.ts
</context>

<acceptance_criteria>

## AC-1: Migration executada
```gherkin
Given o SQL de 0004_anotaai_products.sql
When Doug roda no Supabase Dashboard → SQL Editor
Then as tabelas anotaai_imports e anotaai_products existem no banco sem erro
```

## AC-2: Upload de CSV funciona
```gherkin
Given o app rodando e a migration executada
When Doug sobe um CSV do Anota AI no componente
Then o feedback mostra "X produto(s) importado(s)" e a lista de uploads atualiza
```

## AC-3: Código commitado
```gherkin
Given todos os arquivos do Anota AI não commitados
When o commit é criado
Then git log mostra o commit com todos os arquivos da fase
```

</acceptance_criteria>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-to-do>
    Rodar a migration 0004 no Supabase para criar as tabelas do Anota AI.

    Passos:
    1. Abra o Supabase Dashboard → SQL Editor
    2. Copie e cole o conteúdo de: supabase/migrations/0004_anotaai_products.sql
    3. Clique em RUN
    4. Confirme que o resultado final mostra as duas tabelas com row_count = 0
  </what-to-do>
  <resume-signal>Digite "migration ok" quando terminar</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Componente AnotaaiUploadCard — upload de CSV do Anota AI com seleção de unidade, data do snapshot e lista de últimos imports.</what-built>
  <how-to-verify>
    1. Rode: npm run dev (se não estiver rodando)
    2. Abra o browser no endereço local do app
    3. Role até a seção "Produtos do Delivery (Anota AI)"
    4. Veja se ela aparece com os campos: Unidade, Data do snapshot, Arquivo CSV e botão "Importar produtos"
    5. Se tiver um CSV do Anota AI disponível, faça um upload teste e veja o feedback
    6. Se não tiver CSV, basta confirmar que a seção aparece corretamente visual
  </how-to-verify>
  <resume-signal>Digite "visual ok" se estiver correto, ou descreva o que está errado</resume-signal>
</task>

<task type="auto">
  <name>Commitar todos os arquivos da fase Anota AI</name>
  <files>
    supabase/migrations/0004_anotaai_products.sql,
    src/lib/anotaaiCsv.ts,
    src/api/useAnotaai.ts,
    src/components/AnotaaiUploadCard.tsx,
    src/pages/Dashboard.tsx,
    src/types/database.ts,
    src/index.css,
    PLAN.md
  </files>
  <action>
    Criar commit com todos os arquivos da fase Anota AI.
    Mensagem: "feat(anotaai): upload CSV Anota AI + tabelas de produtos/delivery"
    Inclui: migration SQL, lib de parsing, hook React Query, componente de upload, wire-up no Dashboard, tipos e CSS.
  </action>
  <verify>git log --oneline -1 mostra o commit com a mensagem correta</verify>
  <done>AC-3 satisfeito: arquivos commitados no git</done>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- src/lib/sheets.ts (utilitários de parsing já estáveis)
- src/api/useSales.ts, useAds.ts (outras seções intactas)
- supabase/migrations/0001, 0002, 0003 (migrations anteriores)

## SCOPE LIMITS
- Não adicionar novas features de visualização neste plano (gráficos ficam na Phase 4)
- Não modificar o componente além do que já está escrito
</boundaries>

<verification>
- [ ] Migration executada sem erro no Supabase
- [ ] Seção Anota AI visível e correta no browser
- [ ] `git log --oneline -1` mostra commit da fase
- [ ] `git status` está limpo após commit
</verification>

<success_criteria>
- Tabelas anotaai_imports e anotaai_products criadas no banco
- Componente de upload funcionando visualmente
- Todos os arquivos commitados
</success_criteria>

<output>
Após conclusão, criar `.paul/phases/03-anotaai-delivery/03-01-SUMMARY.md`
</output>
