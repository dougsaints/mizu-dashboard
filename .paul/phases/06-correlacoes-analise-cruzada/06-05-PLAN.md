---
phase: 06-correlacoes-analise-cruzada
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/heatmap.ts
  - src/components/SalesHeatmap.tsx
  - src/components/UnitCompareCard.tsx
  - src/sections/PatternsSection.tsx
  - src/pages/Dashboard.tsx
  - src/index.css
autonomous: true
---

<objective>
## Goal
Portar os 2 últimos gaps do painel HTML (amarelos): **Heatmap semanal de faturamento** (linhas 4455-4614) e **Comparação Justa por Unidade** (linhas 3762-3814). Juntos formam uma nova seção "Padrões e Performance".

## Purpose
- **Heatmap**: dá pulse visual instantâneo de quais dias da semana são fortes/fracos nas últimas 12 semanas. Ajuda Doug a planejar estoque, equipe, promoções por dia.
- **Comparação Justa**: complementa o gráfico de linha (que já existe) com métricas normalizadas: R$/dia, share %, variação vs comparado, média histórica. Permite comparar Serraria (3 canais) com Jatiúca (só PDV) sem viés de canal.

## Output
- `src/lib/heatmap.ts` — funções puras: `startOfWeekSun`, `buildWeekBuckets`, `colorScale`, `daysBetween`
- `src/components/SalesHeatmap.tsx` — heatmap via CSS Grid (não canvas) com tooltip
- `src/components/UnitCompareCard.tsx` — card de comparação por unidade
- `src/sections/PatternsSection.tsx` — orquestra os 2 componentes
- Wire-up no `Dashboard.tsx`
- CSS apendado
- `.paul/phases/06-correlacoes-analise-cruzada/06-05-SUMMARY.md`
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Prior Work
@.paul/phases/06-correlacoes-analise-cruzada/06-04-SUMMARY.md

## Source Files
@src/api/useSales.ts
@src/api/useUnits.ts
@src/lib/period.tsx
@src/lib/aggregation.ts
@src/sections/SalesSection.tsx
@src/pages/Dashboard.tsx
@src/index.css

## Reference HTML
@painel-diario_BKP_2026-05-16_v7-atual.html — linhas 4455-4614 (heatmap completo), 3762-3814 (renderUnitComparison + buildUnitCard), 522-595 (CSS .heatmap-* original)
</context>

<acceptance_criteria>

## AC-1: Helpers de heatmap (puros)

```gherkin
Given uma data anchor e um número de semanas (default 12)
When chamo buildWeekBuckets(rows, anchor, weeks)
Then retorna um array de buckets, cada um com { weekStart: Date, days: [{date, ds, val}] }
  And o array está ordenado da semana mais antiga (índice 0) pra mais recente
  And cada bucket tem exatamente 7 dias (Dom-Sáb), com val=null se a data não está no rows ou é futura
  And startOfWeekSun(date) retorna o domingo da semana de date (timezone local)
  And colorScale(val, min, max) retorna rgb string interpolada entre #EAF6EE (claro) e #27AE60 (verde-escuro), com '#F2F2F4' pra null/0
```

## AC-2: Componente SalesHeatmap

```gherkin
Given rows de sales_daily + range de 12 semanas + canal selecionado
When SalesHeatmap renderiza
Then exibe grid CSS com cabeçalho (Dom..Sáb) + 12 linhas de semanas
  And label da linha = "dd/mmm" (ex: "12/mai")
  And células coloridas por colorScale(val, min, max do conjunto)
  And células sem dado ou futuras: cinza claro (#F2F2F4)
  And hover na célula mostra tooltip absoluto: "Data dd/mm/yyyy · R$ X,XX"
  And legenda no topo direito: "R$ Xk —— R$ Yk" (gradient bar)
  And mobile (<768px): cabeçalho com fontes menores, mantém 7 colunas
  And heatmap respeita filtro de unidade e channel (default 'all' = total); IGNORA range de datas global (sempre últimas 12 semanas)
```

## AC-3: Componente UnitCompareCard

```gherkin
Given uma unidade + rows do período + rows do período comparado + groupTotal do período
When UnitCompareCard renderiza
Then exibe card com:
  - Nome da unidade + share% do período
  - Total R$ no período
  - R$/dia (total / dias com dados) + "{n} dia(s)"
  - Se cmpMode != 'none': badge ▲▼ Δ% vs período comparado + "vs R$ Y"
  - Se a unidade tem ≥8 semanas de histórico, mostra "média histórica: R$ X / dia"
  And cores diferentes por unidade (border-left): Serraria roxo, Jatiúca azul (paleta do SalesLineChart)
```

## AC-4: PatternsSection orquestra os 2

```gherkin
Given useFilters retorna start, end, unitId, channel, cmpMode
When PatternsSection renderiza
Then layout 2 colunas no desktop:
  - Esquerda: SalesHeatmap (alto, ~360px)
  - Direita: lista de UnitCompareCards (1 por unidade ativa)
  And no mobile (<768px), heatmap embaixo, cards em cima
  And busca:
    - rowsAtual = useSales(start, end, { subscribeRealtime: false }) — pra cards
    - rowsCmp = useSalesComparison(start, end, cmpMode) — pra Δ
    - rowsHeatmap = useSales(weeks12_start, today_iso, { subscribeRealtime: false }) — pro heatmap (range próprio independente)
    - rowsHist = useSales(today_minus_8weeks_iso, weeks12_start_minus_1, { subscribeRealtime: false }) OR usa range amplo de 1 ano... — decisão: pra simplificar, calcular histRate a partir do MESMO heatmap range (12 semanas) — fiel ao spirit ("≥8 semanas")
```

## AC-5: Visual integrado

```gherkin
Given a seção renderiza
When visualizo
Then segue padrão `.mizu-section` (kanji 律 = ritmo/padrão)
  And renderiza após `ProductsAnalysisSection`, antes de `RoiSection`
  And lazy + Suspense individual
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: lib/heatmap.ts — funções puras</name>
  <files>src/lib/heatmap.ts</files>
  <action>
    ```ts
    export type HeatmapDay = { date: Date; ds: string; val: number | null }
    export type HeatmapBucket = { weekStart: Date; days: HeatmapDay[] }

    export function startOfWeekSun(d: Date): Date {
      const x = new Date(d)
      x.setHours(0, 0, 0, 0)
      x.setDate(x.getDate() - x.getDay())
      return x
    }

    export function toIso(d: Date): string {
      // yyyy-mm-dd
      const y = d.getFullYear()
      const m = String(d.getMonth() + 1).padStart(2, '0')
      const day = String(d.getDate()).padStart(2, '0')
      return `${y}-${m}-${day}`
    }

    export function buildWeekBuckets(
      daily: Array<{ ds: string; val: number }>,
      anchor: Date,
      weeks: number = 12,
    ): HeatmapBucket[] {
      const byDate = new Map<string, number>()
      for (const d of daily) byDate.set(d.ds, d.val)

      const lastSun = startOfWeekSun(anchor)
      const out: HeatmapBucket[] = []
      for (let w = weeks - 1; w >= 0; w--) {
        const ws = new Date(lastSun)
        ws.setDate(ws.getDate() - w * 7)
        const days: HeatmapDay[] = []
        for (let i = 0; i < 7; i++) {
          const day = new Date(ws)
          day.setDate(day.getDate() + i)
          const ds = toIso(day)
          const v = byDate.get(ds)
          days.push({ date: day, ds, val: v == null ? null : v })
        }
        out.push({ weekStart: ws, days })
      }
      return out
    }

    export function colorScale(val: number | null, min: number, max: number): string {
      if (val == null || val <= 0) return '#F2F2F4'
      if (max <= min) return '#27AE60'
      const t = Math.min(1, Math.max(0, (val - min) / (max - min)))
      const c0 = [0xEA, 0xF6, 0xEE]
      const c1 = [0x27, 0xAE, 0x60]
      const r = Math.round(c0[0] + (c1[0] - c0[0]) * t)
      const g = Math.round(c0[1] + (c1[1] - c0[1]) * t)
      const b = Math.round(c0[2] + (c1[2] - c0[2]) * t)
      return `rgb(${r}, ${g}, ${b})`
    }

    export function daysBetween(startIso: string, endIso: string): number {
      const s = new Date(startIso + 'T00:00:00')
      const e = new Date(endIso + 'T00:00:00')
      return Math.round((e.getTime() - s.getTime()) / 86400000) + 1
    }
    ```
  </action>
  <verify>`npx tsc --noEmit` passa.</verify>
  <done>AC-1 satisfeito.</done>
</task>

<task type="auto">
  <name>Task 2: SalesHeatmap.tsx — grid CSS com tooltip</name>
  <files>src/components/SalesHeatmap.tsx</files>
  <action>
    Componente `<SalesHeatmap rows={SalesRow[]} channel={Channel} />`:

    1. Calcula:
       - `maxDate` = max(rows.date as Date)
       - `daily[]` = soma por `date` (aplicando channel: if 'all' → row.total, senão row[channel])
       - `buckets` = buildWeekBuckets(daily, maxDate, 12)
       - `min/max` = min/max dos vals positivos em todos os buckets
    2. Layout:
       ```tsx
       <div className="heatmap-wrap">
         <div className="heatmap-legend">
           <span>{R$k(min)}</span>
           <span className="heatmap-legend-bar" />
           <span>{R$k(max)}</span>
         </div>
         <div className="heatmap">
           <div className="heatmap-corner" />
           {DOW_LABELS.map(l => <div className="heatmap-col-label" key={l}>{l}</div>)}
           {buckets.map((b, bi) => (
             <Fragment key={bi}>
               <div className="heatmap-row-label">{formatRowLabel(b.weekStart)}</div>
               {b.days.map((d, di) => (
                 <Cell key={di} day={d} max={maxDate} min={min} maxVal={max} />
               ))}
             </Fragment>
           ))}
         </div>
       </div>
       ```
    3. Cell:
       - background: colorScale OU 'empty' se future/null
       - onMouseEnter: setTooltip({ html, x, y }) — tooltip por hover state simples
       - onMouseLeave: setTooltip(null)
    4. Tooltip via state local + position absolute (fixed) — não DOM manipulation. Mostrar Data dd/mm/yyyy + R$ X,XX.
    5. CSS grid: `grid-template-columns: 56px repeat(7, 1fr); gap: 3px;`
    6. R$k helper: 1234 → "R$ 1,2k", 1234567 → "R$ 1,2M"
    7. formatRowLabel(d): "12/mai" (dia + mes 3 letras lowercase)
    8. Se 0 rows ou maxVal=0: mostra "heatmap-empty-state"

    Avoid: canvas (CSS Grid puro é suficiente, mais simples e acessível).
  </action>
  <verify>Build passa, visual depende do Doug.</verify>
  <done>AC-2 satisfeito.</done>
</task>

<task type="auto">
  <name>Task 3: UnitCompareCard.tsx</name>
  <files>src/components/UnitCompareCard.tsx</files>
  <action>
    Componente `<UnitCompareCard unit={Unit} rowsCurr={SalesRow[]} rowsPrev={SalesRow[]} channel={Channel} comparing={boolean} groupTotal={number} histRate={number | null} />`:

    - Filtra rowsCurr/Prev por unit.id internamente
    - Aplica channel pra computar total (igual SalesSection)
    - Calcula: total, dias, rate, share, prevTotal, delta via `computeDelta` (importar de aggregation.ts)
    - Renderiza:
      ```tsx
      <div className={`unit-compare-card uc-${unit.display_name.toLowerCase()}`}>
        <div className="uc-head">
          <span className="uc-name">{unit.display_name}</span>
          <span className="uc-share">{share.toFixed(1)}% do período</span>
        </div>
        <div className="uc-total">{brl(total)}</div>
        <div className="uc-rate">{brl(rate)} / dia • {days} dia(s)</div>
        {comparing && delta.pct !== null && (
          <div className="uc-meta">
            <DeltaBadge ... />
            <span className="uc-prev">vs {brl(prevTotal)}</span>
          </div>
        )}
        {histRate !== null && (
          <div className="uc-ref">— média histórica: {brl(histRate)} / dia (≥8 semanas)</div>
        )}
      </div>
      ```
    - Cores per unit no border-left via class derivada do nome (paleta paralela ao SalesLineChart)
  </action>
  <verify>Build passa.</verify>
  <done>AC-3 satisfeito.</done>
</task>

<task type="auto">
  <name>Task 4: PatternsSection.tsx — orquestrador</name>
  <files>src/sections/PatternsSection.tsx</files>
  <action>
    1. Imports: `useFilters`, `useSales`, `useSalesComparison`, `useUnits`, `startOfWeekSun, toIso, daysBetween` de `heatmap.ts`, `SalesHeatmap`, `UnitCompareCard`
    2. Calcula range das 12 semanas (12 * 7 dias a partir de hoje):
       ```ts
       const today = useMemo(() => new Date(), [])
       const heatmapEndIso = useMemo(() => toIso(today), [today])
       const heatmapStartIso = useMemo(() => {
         const start = startOfWeekSun(today)
         start.setDate(start.getDate() - 11 * 7) // 12 semanas inclui a atual
         return toIso(start)
       }, [today])
       ```
    3. Busca 3 queries:
       - `rowsAtual = useSales(start, end, { subscribeRealtime: false })`
       - `rowsCmp = useSalesComparison(start, end, cmpMode)`
       - `rowsHeatmap = useSales(heatmapStartIso, heatmapEndIso, { subscribeRealtime: false })`
    4. Filtra rowsHeatmap por unitId (se aplicável). Passa pro SalesHeatmap.
    5. groupTotal = soma de rowsAtual.total
    6. histRate por unidade: a partir de rowsHeatmap (12 semanas = ≥8). Função inline `computeHistRate(rows, unitId)` que retorna total/days OU null se < 8 semanas (12 weeks * 7 = 84 dias, requerimento: 8*7 = 56 dias com dados).
    7. Renderiza 2 colunas:
       ```tsx
       <section className="mizu-section">
         <div className="mizu-section-head">
           <div>
             <div className="mizu-section-title">
               <span className="kanji-deco">律</span> Padrões e Performance
             </div>
             <div className="mizu-section-sub">
               Padrão semanal de faturamento (últimas 12 semanas) e comparação normalizada entre unidades
             </div>
           </div>
         </div>
         <div className="patterns-grid">
           <div className="chart-card patterns-heatmap-card">
             <div className="chart-card-title">Padrão Semanal de Faturamento</div>
             <div className="chart-card-sub">12 semanas · cor mais escura = dia mais forte</div>
             <SalesHeatmap rows={rowsHeatmapFiltered} channel={channel} />
           </div>
           <div className="patterns-units">
             {activeUnits.map(u => (
               <UnitCompareCard key={u.id} unit={u} ... />
             ))}
           </div>
         </div>
       </section>
       ```
  </action>
  <verify>Build passa.</verify>
  <done>AC-4 satisfeito.</done>
</task>

<task type="auto">
  <name>Task 5: Wire-up Dashboard + CSS</name>
  <files>src/pages/Dashboard.tsx, src/index.css</files>
  <action>
    1. `Dashboard.tsx`:
       - `const PatternsSection = lazy(() => import('../sections/PatternsSection'))`
       - Renderiza após ProductsAnalysisSection, antes de RoiSection

    2. `index.css` — apendar. Inspirado nas linhas 522-595 do HTML original:
       - `.patterns-grid` 2 colunas (1fr 1fr) → 1 coluna mobile
       - `.patterns-heatmap-card` padding maior
       - `.patterns-units { display: flex; flex-direction: column; gap: 12px; }`
       - `.heatmap-wrap { position: relative; }`
       - `.heatmap { display: grid; grid-template-columns: 56px repeat(7, 1fr); gap: 3px; }`
       - `.heatmap-corner` vazio
       - `.heatmap-col-label, .heatmap-row-label { font-size: 11px; color: var(--txt-2); }`
       - `.heatmap-col-label { font-weight: 600; padding-bottom: 4px; text-align: center; }`
       - `.heatmap-row-label { text-align: right; padding-right: 8px; align-self: center; }`
       - `.heatmap-cell { height: 28px; border-radius: 4px; cursor: pointer; transition: outline 0.1s; }`
       - `.heatmap-cell:hover { outline: 2px solid var(--txt); outline-offset: -1px; }`
       - `.heatmap-cell.empty { background: #F2F2F4; cursor: default; }`
       - `.heatmap-empty-state` mensagem central
       - `.heatmap-legend` flex no header do card, com `.heatmap-legend-bar` gradient
       - `.heatmap-tooltip` fixed, z-index alto, com transição
       - UnitCompareCard:
         - `.unit-compare-card { background: var(--card); border: 1px solid var(--border); border-left: 4px solid var(--border); border-radius: var(--r-md); padding: 14px 16px; }`
         - `.uc-serraria { border-left-color: #8E44AD; }` (paleta SalesLineChart)
         - `.uc-jatiuca { border-left-color: #2980B9; }`
         - `.uc-head, .uc-total, .uc-rate, .uc-meta, .uc-ref` — estilos coerentes
       - Mobile: `.patterns-grid { grid-template-columns: 1fr; } .heatmap-cell { height: 24px; }`
  </action>
  <verify>Build verde, chunk dedicado.</verify>
  <done>AC-5 satisfeito.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `useSales`, `useSalesComparison`
- `aggregation.ts` (já criado em 06-03)
- Outras sections

## SCOPE LIMITS
- Heatmap só pra Vendas (não pra Meta Ads / Anota AI). Se Doug quiser depois, é plan novo.
- Sem médias por dia da semana (cabeçalho extra "Dom: R$ X média") — não estava nas linhas que documentei. Pode adicionar como deferred.
- histRate só do range de 12 semanas (não busca histórico mais profundo). Documentar como "média das 12 semanas exibidas" no SUMMARY se preciso.

</boundaries>

<verification>
- [ ] tsc --noEmit passa
- [ ] build passa, chunk dedicado pra PatternsSection
- [ ] Heatmap renderiza 12 linhas × 7 colunas
- [ ] Tooltip aparece no hover
- [ ] UnitCompareCards com cores diferentes (Serraria roxo, Jatiúca azul)
- [ ] Mobile responsivo
</verification>

<success_criteria>
- Build verde
- Phase 6 fecha em 100% (5/5 plans)
- Heatmap visualmente similar ao painel original
- Comparação justa com R$/dia, share, delta, hist
</success_criteria>

<output>
`.paul/phases/06-correlacoes-analise-cruzada/06-05-SUMMARY.md`
</output>
