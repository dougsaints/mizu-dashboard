---
phase: 10-densidade-identidade-visual
plan: 03
type: execute
wave: 1
depends_on: ["10-02"]
files_modified:
  - src/components/SalesLineChart.tsx
  - src/sections/AnalysisSection.tsx
  - src/sections/SalesSection.tsx
  - src/index.css
autonomous: true
---

<objective>
## Goal
Aplicar **cor consistente por unidade** (Jatiúca azul, Serraria roxo) em todos os charts e cards onde unidade aparece. Adicionar **badge colorida da unidade** no `.unit-card` da SalesSection — chip "JATIÚCA"/"SERRARIA" identificando visualmente.

## Purpose
ROADMAP da v0.2 manda: "Cores por unidade consistentes em todo painel". Hoje SalesLineChart usa laranja+azul (#e8623d / #3d8be8), AnalysisSection bars também — descasa do padrão azul+roxo já usado em .unit-card e .ads-unit-card. Plus, .unit-card hoje só tem padding/total/breakdown — sem identidade visual da unidade no miolo.

## Output
- 2 arquivos .tsx com cores trocadas pra tokens de unidade
- src/sections/SalesSection.tsx com badge inline no unit-card
- index.css com classes .unit-card-badge + border-left grosso
</objective>

<context>
@src/components/SalesLineChart.tsx
@src/sections/AnalysisSection.tsx
@src/sections/SalesSection.tsx
@src/index.css
</context>

<acceptance_criteria>

## AC-1: Charts de Vendas usam cor padrão de unidade
```gherkin
Given SalesLineChart e AnalysisSection (faturamento por período)
When inspeciono as cores
Then Jatiúca aparece azul (#2980B9 ou var unit-jatiuca)
And Serraria aparece roxo (#8E44AD ou var unit-serraria)
And não há mais laranja como cor de unidade nesses gráficos
```

## AC-2: unit-card com badge da unidade visível
```gherkin
Given a SalesSection renderizada
When olho cada unit-card (Serraria, Jatiúca)
Then cada card mostra um chip pequeno em caixa alta com o nome da unidade na cor correspondente (badge no canto superior direito ou ao lado do nome)
And border-left do unit-card cresceu de 4px pra 6px na cor da unidade
```

## AC-3: Build limpo
```gherkin
Given mudanças
When rodo npx tsc --noEmit && npm run build
Then sem erros
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Padronizar cores de unidade nos charts</name>
  <files>src/components/SalesLineChart.tsx, src/sections/AnalysisSection.tsx</files>
  <action>
    **`SalesLineChart.tsx`** (~line 29):
    Substituir:
    ```ts
    const PALETTE = ['#e8623d', '#3d8be8', '#3dba8b', '#b88a2e']
    ```
    Por:
    ```ts
    // Cores fixas por posição: Serraria roxo (1ª na lista por sort_order),
    // Jatiúca azul (2ª). Fallback pra outras unidades futuras.
    const PALETTE = ['#8E44AD', '#2980B9', '#3dba8b', '#b88a2e']
    ```

    **`AnalysisSection.tsx`** (~line 30):
    Substituir:
    ```ts
    const COLOR_SERRARIA = '#e8623d'
    const COLOR_JATIUCA = '#3d8be8'
    ```
    Por:
    ```ts
    const COLOR_SERRARIA = '#8E44AD'
    const COLOR_JATIUCA = '#2980B9'
    ```

    Mantém `COLOR_ADS = '#b88a2e'` (gold deep, ok pra Meta Ads no chart de barras).
  </action>
  <verify>grep -rn "#e8623d\|#3d8be8" src/components/ src/sections/ — zero matches em arquivo nosso (tira o lib/ que pode ter pra outras coisas)</verify>
  <done>AC-1 satisfeito</done>
</task>

<task type="auto">
  <name>Task 2: Badge da unidade no unit-card</name>
  <files>src/sections/SalesSection.tsx, src/index.css</files>
  <action>
    **No `SalesSection.tsx`**, dentro do `.map` de `aggregated`:
    Adicionar após `<div className="unit-card-name">{unitName(id)}</div>`:
    ```tsx
    <div className={`unit-card-badge unit-card-badge--${unitName(id).toLowerCase().includes('jatiuca') || unitName(id).toLowerCase().includes('jatiúca') ? 'jatiuca' : unitName(id).toLowerCase().includes('serraria') ? 'serraria' : 'other'}`}>
      {unitName(id).toUpperCase()}
    </div>
    ```

    Atenção: já existe `.unit-card-name` mostrando o nome. A badge é redundante — deixa só a badge (visualmente colorida e em caps), removendo o `.unit-card-name` ou reposicionando.

    Decisão pragmática: SUBSTITUIR `<div className="unit-card-name">{unitName(id)}</div>` por:
    ```tsx
    <div className={`unit-card-badge unit-card-badge--${
      unitName(id).toLowerCase().includes('jatiu') ? 'jatiuca' :
      unitName(id).toLowerCase().includes('serraria') ? 'serraria' : 'other'
    }`}>
      {unitName(id).toUpperCase()}
    </div>
    ```

    **No `index.css`**, appendar no fim do arquivo (logo antes do bloco `.section-header` que está no final):

    ```css
    /* --- Unit card badge (Phase 10-03) ---------------------- */
    .unit-card { border-left-width: 6px !important; }
    .unit-card-badge {
      display: inline-block;
      font-size: 10px;
      font-weight: 800;
      letter-spacing: 1px;
      text-transform: uppercase;
      padding: 3px 8px;
      border-radius: 999px;
      margin-bottom: 8px;
      background: var(--mizu-cream);
      color: var(--txt-2);
      border: 1px solid var(--border);
    }
    .unit-card-badge--jatiuca {
      background: var(--unit-jatiuca-soft);
      color: var(--unit-jatiuca);
      border-color: rgba(41,128,185,0.30);
    }
    .unit-card-badge--serraria {
      background: var(--unit-serraria-soft);
      color: var(--unit-serraria);
      border-color: rgba(142,68,173,0.30);
    }
    /* Reset border-left por unidade — se .unit-card.serraria/.jatiuca existir */
    .unit-card.serraria { border-left-color: var(--unit-serraria) !important; }
    .unit-card.jatiuca  { border-left-color: var(--unit-jatiuca) !important; }
    ```
  </action>
  <verify>npx tsc --noEmit limpo; grep unit-card-badge src/sections/SalesSection.tsx encontra 1+ matches</verify>
  <done>AC-2 satisfeito</done>
</task>

<task type="auto">
  <name>Task 3: Build final</name>
  <files>—</files>
  <action>Rodar `npx tsc --noEmit && npm run build` e confirmar sucesso.</action>
  <verify>build conclui sem erros</verify>
  <done>AC-3 satisfeito</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- supabase/migrations/*
- src/api/* (hooks)
- ProductsAnalysisSection, MetaAdsAnalysisSection (cores de campanha são definidas em lib/adsCategory.ts — fora do escopo)
- SectionHeader e seu CSS (10-01)
- KPI compactação (10-02)

## SCOPE LIMITS
- SÓ unit colors em SalesLineChart, AnalysisSection, unit-card.
- Não criar badge no .ads-unit-card (já tem cor via .ads-unit-card.jatiuca/.serraria — já consistente).
- Não touch nas cores de chart-cards de produtos (Pareto, donut categoria).

</boundaries>

<verification>
- [ ] Build limpo
- [ ] Padrão visual azul (Jatiúca) + roxo (Serraria) consistente nos charts e .unit-card
- [ ] Verificação visual final adiada pra fim da Phase 10
</verification>

<output>
SUMMARY em `.paul/phases/10-densidade-identidade-visual/10-03-SUMMARY.md`.
</output>
