---
phase: 10-densidade-identidade-visual
plan: 04
type: execute
wave: 1
depends_on: ["10-03"]
files_modified:
  - src/sections/WeeklyRecap.tsx
  - src/sections/MarketingUnif.tsx
  - src/sections/RoiSection.tsx
  - src/index.css
autonomous: true
---

<objective>
## Goal
Adicionar **storytelling em prosa** no topo de 3 seções-chave (WeeklyRecap, MarketingUnif, RoiSection) — frase curta de 1-2 linhas explicando o que os números estão dizendo. Replicar o pattern já usado em `TrendsSection.buildSummary`.

## Purpose
ROADMAP da v0.2 pede: "Storytelling em prosa nos heroes (replicar pattern do TrendsSection.buildSummary)". O painel HTML antigo tem esse pattern e Doug citou ("Campanhas atribuídas à Jatiúca alcançaram 14.202 pessoas, 20% do alcance total..."). Hoje no React, números aparecem nus sem contexto narrativo — dificulta leitura rápida pro time leigo em dados.

## Output
- 3 .tsx com bloco `.hero-summary` renderizado entre header e grid de KPIs
- CSS `.hero-summary` no index.css
</objective>

<context>
@src/sections/WeeklyRecap.tsx
@src/sections/MarketingUnif.tsx
@src/sections/RoiSection.tsx
@src/sections/TrendsSection.tsx
</context>

<acceptance_criteria>

## AC-1: WeeklyRecap com frase-resumo
Frase tipo "Faturamento da semana: X. [alerta de queda se houver]. ROAS de Yx sobre R$ Z investidos." aparece acima do wr-grid.

## AC-2: MarketingUnif com frase-resumo
Frase tipo "Em N dias o Instagram alcançou X pessoas. Z% desse alcance veio de campanhas pagas (R$ W investidos)." aparece acima do mkt-block-label.

## AC-3: RoiSection com frase-resumo
Frase tipo "Você investiu R$ X em marketing no [mês/semana] e faturou R$ Y, com margem de R$ Z (ROAS Wx)." aparece acima do roi-grid.

## AC-4: Build limpo + CSS apropriado
- npx tsc --noEmit limpo
- `.hero-summary` no CSS com tipografia clara (font-size ~14-15px, weight 500, line-height 1.5, padding razoável)

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: hero-summary no WeeklyRecap</name>
  <files>src/sections/WeeklyRecap.tsx</files>
  <action>
    Após o <SectionHeader/> e antes do <div className="wr-grid">, adicionar:

    ```tsx
    {!isLoading && data && (
      <div className="hero-summary">
        {(() => {
          const totalAtual = data.units.reduce((s, u) => s + u.cur, 0)
          const totalAnterior = data.units.reduce((s, u) => s + u.prev, 0)
          const delta = totalAnterior > 0 ? ((totalAtual - totalAnterior) / totalAnterior) * 100 : null
          const deltaStr = delta == null ? '' : delta > 2 ? ` (▲ ${delta.toFixed(0)}% vs semana passada)` : delta < -2 ? ` (▼ ${Math.abs(delta).toFixed(0)}% vs semana passada)` : ' (estável vs semana passada)'
          const alertStr = data.alertDown ? ` ${data.alertDown.unit} caiu ${Math.abs(data.alertDown.pct).toFixed(0)}% — vale investigar.` : ''
          const roasStr = data.roas != null && data.adSpend > 0 ? ` ROAS de ${data.roas.toFixed(2)}x sobre ${brl(data.adSpend)} investidos no Meta Ads.` : ''
          return (
            <>
              Faturamento da semana: <strong>{brl(totalAtual)}</strong>{deltaStr}.{alertStr}{roasStr}
            </>
          )
        })()}
      </div>
    )}
    ```

    Garantir que a função `brl` está disponível no escopo (já está, linha 3-5).
  </action>
  <verify>grep hero-summary src/sections/WeeklyRecap.tsx — 1 match</verify>
  <done>AC-1 satisfeito</done>
</task>

<task type="auto">
  <name>Task 2: hero-summary no MarketingUnif</name>
  <files>src/sections/MarketingUnif.tsx</files>
  <action>
    Após o <SectionHeader/> e antes do error/empty/main render, adicionar antes do `<div className="mkt-block-label">`:

    ```tsx
    {!isLoading && hasData && (
      <div className="hero-summary">
        Em <strong>{organic.length} dia(s)</strong>, o Instagram do Sushi Mizú alcançou <strong>{int(totals.alcance)}</strong> pessoas com <strong>{int(totals.visualizacoes)}</strong> visualizações totais.
        {paidPct != null && paidPct > 0 && (
          <> Cerca de <strong>{paidPct.toFixed(0)}%</strong> desse alcance veio de campanhas pagas ({brl(adSpend)} investidos).</>
        )}
        {paidPct === null && adSpend > 0 && (
          <> Houve {brl(adSpend)} investidos em Meta Ads no período (alcance não calculável).</>
        )}
      </div>
    )}
    ```
  </action>
  <verify>grep hero-summary src/sections/MarketingUnif.tsx — 1 match</verify>
  <done>AC-2 satisfeito</done>
</task>

<task type="auto">
  <name>Task 3: hero-summary no RoiSection</name>
  <files>src/sections/RoiSection.tsx</files>
  <action>
    Após o <SectionHeader/> e antes do <div className="roi-grid">, adicionar:

    ```tsx
    {!isLoading && (
      <div className="hero-summary">
        {invest > 0 && fat > 0 ? (
          <>
            Você investiu <strong>{brl(invest)}</strong> em marketing {periodLbl2} e faturou <strong>{brl(fat)}</strong>, com margem de <strong>{brl(margem)}</strong> (ROAS <strong>{roas.toFixed(2)}x</strong>).
          </>
        ) : invest > 0 ? (
          <>
            Investimento configurado: <strong>{brl(invest)}</strong> {periodLbl}. Aguardando faturamento {periodLbl2} pra calcular margem e ROAS.
          </>
        ) : (
          <>Configure os valores de investimento abaixo pra ver o resumo do ROI.</>
        )}
      </div>
    )}
    ```
  </action>
  <verify>grep hero-summary src/sections/RoiSection.tsx — 1 match</verify>
  <done>AC-3 satisfeito</done>
</task>

<task type="auto">
  <name>Task 4: CSS .hero-summary + build final</name>
  <files>src/index.css</files>
  <action>
    Appendar no fim do arquivo:

    ```css
    /* ============================================================
       Hero summary — Phase 10-04
       Frase-resumo em prosa no topo de seções-chave (WeeklyRecap,
       MarketingUnif, RoiSection). Padrão do painel HTML antigo.
    ============================================================ */
    .hero-summary {
      font-size: 14px;
      font-weight: 500;
      color: var(--txt);
      line-height: 1.55;
      padding: 12px 16px;
      margin: 0 0 16px;
      background: var(--mizu-cream);
      border-left: 3px solid var(--mizu-gold);
      border-radius: 6px;
    }
    .hero-summary strong {
      font-weight: 800;
      color: var(--mizu-ink);
    }
    /* Variantes por fonte — herda do is-source-X do .mizu-section pai */
    .mizu-section.is-source-meta      .hero-summary { border-left-color: var(--source-meta); }
    .mizu-section.is-source-anotaai   .hero-summary { border-left-color: var(--source-anotaai); }
    .mizu-section.is-source-instagram .hero-summary { border-left-color: var(--source-instagram); }
    .mizu-section.is-source-vendas    .hero-summary { border-left-color: var(--source-vendas); }
    .mizu-section.is-source-diario    .hero-summary { border-left-color: var(--source-diario); }
    ```

    Depois rodar `npx tsc --noEmit && npm run build`.
  </action>
  <verify>Build limpo</verify>
  <done>AC-4 satisfeito</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Hooks (useWeeklyRecap, useOrganic, useAds, useRoi)
- Lógica de cálculo existente — só LER os valores já computados
- Componentes (.wr-block, .mkt-tile, .roi-metric)
- Header de seção (10-01)
- Cores de unidade (10-03)

## SCOPE LIMITS
- 3 seções apenas (WeeklyRecap, MarketingUnif, RoiSection)
- Sem novas dependências
- Frase em PT-BR, formato simples (sem markdown, sem links)
- Não adicionar storytelling em outras seções (TrendsSection já tem; outras ficam pra plans futuros se Doug pedir)

</boundaries>

<verification>
- [ ] npx tsc --noEmit limpo
- [ ] npm run build limpo
- [ ] 3 ocorrências de hero-summary nas 3 seções
- [ ] Verificação visual final adiada
</verification>

<success_criteria>
- 3 seções com frase em prosa renderizada
- CSS .hero-summary uniforme com variantes por source
- Build limpo
</success_criteria>

<output>
SUMMARY em 10-04-SUMMARY.md
</output>
