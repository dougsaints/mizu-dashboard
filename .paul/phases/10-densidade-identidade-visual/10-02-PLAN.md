---
phase: 10-densidade-identidade-visual
plan: 02
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - src/index.css
autonomous: true
---

<objective>
## Goal
Compactar os KPI cards das 6 seções principais — fim do "espaço vazio" que o painel HTML antigo (referência) não tem. Reduzir padding/min-height + aumentar o número principal pra criar hierarquia mais forte.

## Purpose
Doug detectou que o painel React ainda parece "espaçoso demais" comparado à referência. KPI cards com altura excessiva e número pequeno desperdiçam densidade da informação. Esse plan ataca isso só no CSS, sem mexer em componente nenhum.

## Output
- `src/index.css` ajustado em ~6 blocos (kpi-card, trend-card, wr-block, mkt-tile, roi-kpi, jat-kpi)
- `SUMMARY.md` em `.paul/phases/10-densidade-identidade-visual/10-02-SUMMARY.md`
</objective>

<context>
@.paul/PROJECT.md
@src/index.css
</context>

<acceptance_criteria>

## AC-1: Padding reduzido nos KPI cards principais
```gherkin
Given os blocos .kpi-card, .trend-card, .mkt-tile, .wr-block no index.css
When inspeciono os valores de padding e min-height
Then padding interno diminuiu (≥4px menos) e min-height de .trend-card removido (deixa altura natural)
And o conteúdo continua respirando (não fica grudado)
```

## AC-2: Número principal maior nos KPIs
```gherkin
Given os blocos .kpi-val, .trend-card-value, .mkt-tile-val, .wr-roas-val
When inspeciono font-size
Then o número principal cresceu pra criar hierarquia clara (>= 26px no main, >= 32px nos hero)
And label/subtext continua menor pra ressaltar o número
```

## AC-3: Build limpo
```gherkin
Given as mudanças
When rodo npx tsc --noEmit e npm run build
Then ambos completam sem erros nem warnings novos
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Reduzir padding e min-height + aumentar fontes nos KPIs</name>
  <files>src/index.css</files>
  <action>
    Aplicar os seguintes ajustes específicos:

    **`.kpi-card`** (linha ~414): `padding: 20px` → `padding: 16px 18px`
    **`.kpi-icon`** (linha ~448): `margin-bottom: 14px` → `margin-bottom: 8px`; `width/height: 36px` → `width/height: 30px`
    **`.kpi-val`** (linha ~460): `font-size: 24px` → `font-size: 28px`; `margin-bottom: 6px` → `margin-bottom: 4px`
    **`.kpi-label`** (linha ~455): `margin-bottom: 6px` → `margin-bottom: 4px`

    **`.trend-card`** (linha ~2256): `padding: 14px 16px` → `padding: 12px 14px`; `min-height: 96px` → REMOVER (deixa natural); `gap: 4px` → `gap: 2px`
    **`.trend-card-value`** (linha ~2281): `font-size: 22px` → `font-size: 26px`

    **`.mkt-tile`** (linha ~1947): `padding: 12px 14px` → `padding: 10px 12px`
    **`.mkt-tile-val`** (linha ~1952): `font-size: 22px` → `font-size: 26px`; `margin-top: 6px` no `.mkt-tile-lbl` → `margin-top: 4px`

    **`.wr-block`** (linha ~1302 e ~1883 — pode haver 2 definições; ajustar AMBAS se ambas tiverem padding):
    - `padding: 14px 16px` → `padding: 12px 14px`

    **`.wr-unit-val`** (linha ~1297 ou perto): `font-size: 22px` → `font-size: 26px`

    **`.jat-kpi`** (linha ~971): `padding: 12px` → `padding: 10px 12px` (se tiver padding)

    **Boundaries:**
    - NÃO mexer em chart-card (gráficos têm dimensão própria, mexer quebra layout dos gráficos)
    - NÃO mexer em .mizu-section (já trabalhado em 10-01)
    - NÃO criar tokens novos; usar valores literais nessa fatia
    - Se alguma classe não existir exatamente como descrito, ajustar a mais próxima (verificar com Read se tiver dúvida)
  </action>
  <verify>npx tsc --noEmit && npm run build — sem erros</verify>
  <done>AC-1 + AC-2 + AC-3 satisfeitos</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/components/* (nenhum .tsx)
- src/sections/* (nenhum .tsx)
- supabase/migrations/*
- Tokens do :root (criados em 10-01)
- .mizu-section, .section-header* (consolidados em 10-01)
- Chart canvas containers (gráficos dependem de altura específica)

## SCOPE LIMITS
- SÓ CSS de KPI cards listados nas Task action
- SEM criar componente novo
- SEM mexer em comportamento responsive (media queries existentes ficam)

</boundaries>

<verification>
- [ ] npx tsc --noEmit limpo
- [ ] npm run build limpo
- [ ] Diferença visível no browser (será verificado por Doug ao fim da Phase 10)
</verification>

<success_criteria>
- 8-10 valores CSS ajustados
- Build limpo
- AC-1, AC-2, AC-3 satisfeitos
</success_criteria>

<output>
Criar `.paul/phases/10-densidade-identidade-visual/10-02-SUMMARY.md` com o resumo das mudanças e tamanhos finais.
</output>
