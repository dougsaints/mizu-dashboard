---
phase: 12-organizacao-mobile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Dashboard.tsx
  - src/index.css
autonomous: true
---

<objective>
## Goal
Reordenar seções do Dashboard pela ordem natural de leitura (Tendências → Vendas → Meta → Delivery → Orgânico → Padrões → Diário → Uploads) e separar **3 upload cards** (Meta, AnotaAi, Instagram) do scroll principal — agrupar num bloco "Configurações & Uploads" no rodapé, visualmente discreto.

## Output
- Dashboard.tsx com ordem reorganizada
- Bloco `<details>` colapsável no fim contendo os 3 upload cards
- CSS pra o agrupamento
</objective>

<context>
@src/pages/Dashboard.tsx
@.paul/ROADMAP.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Reordenar + agrupar uploads no Dashboard.tsx</name>
  <files>src/pages/Dashboard.tsx</files>
  <action>
    Reordenar pra:
    1. WeeklyRecap (resumo)
    2. TrendsSection
    3. SalesSection
    4. AnalysisSection (vendas mensal/semanal)
    5. MetaAdsAnalysisSection
    6. RoiSection
    7. ProductsAnalysisSection (Anota AI delivery)
    8. MarketingUnif (Instagram orgânico)
    9. PatternsSection
    10. CorrelationSection
    11. DataTableSection
    12. DiarioSection
    13. **Bloco colapsável "Configurações & Uploads"** contendo AdsUploadCard, AnotaaiUploadCard, InstagramUploadCard

    Usar `<details className="uploads-area">` com `<summary>` "⚙ Configurações & Uploads" e os 3 cards dentro.
  </action>
  <verify>npx tsc --noEmit limpo; ordem das sections no Dashboard.tsx coincide</verify>
  <done>Reorder aplicado, uploads dentro de details</done>
</task>

<task type="auto">
  <name>Task 2: CSS pra uploads-area</name>
  <files>src/index.css</files>
  <action>
    Appendar:
    ```css
    /* ============================================================
       Uploads area — Phase 12-01 (discreta no rodapé)
    ============================================================ */
    .uploads-area {
      margin-top: 32px;
      padding-top: 24px;
      border-top: 1px dashed var(--border);
    }
    .uploads-area > summary {
      cursor: pointer;
      font-size: 13px;
      font-weight: 700;
      letter-spacing: 0.4px;
      color: var(--txt-2);
      padding: 8px 12px;
      background: var(--mizu-cream);
      border: 1px solid var(--border);
      border-radius: 8px;
      list-style: none;
      user-select: none;
      transition: background 0.15s;
    }
    .uploads-area > summary:hover { background: #F0EBD9; }
    .uploads-area > summary::-webkit-details-marker { display: none; }
    .uploads-area > summary::before {
      content: '▸';
      display: inline-block;
      margin-right: 8px;
      transition: transform 0.15s;
      color: var(--mizu-gold);
    }
    .uploads-area[open] > summary::before {
      transform: rotate(90deg);
    }
    .uploads-area-inner {
      padding-top: 16px;
    }
    ```
  </action>
  <verify>build limpo</verify>
  <done>CSS appendado</done>
</task>
</tasks>

<boundaries>
- NÃO modificar os Upload cards em si (só re-locar)
- NÃO mexer no FilterProvider/Header
- NÃO criar componente novo (uses details/summary HTML nativo)
</boundaries>

<output>SUMMARY 12-01-SUMMARY.md</output>
