---
phase: 12-organizacao-mobile
plan: 02
type: execute
wave: 1
depends_on: ["12-01"]
files_modified:
  - src/index.css
autonomous: true
---

<objective>
## Goal
Bloco final de media queries pra mobile: reflow em 1 coluna < 640px, touch targets ≥ 44px, padding reduzido nas seções, header colapsável.

## Output
Bloco CSS appendado no fim do index.css.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Mobile compactness pass</name>
  <files>src/index.css</files>
  <action>
    Appendar no fim:
    ```css
    /* ============================================================
       Mobile compactness pass — Phase 12-02
       Touch targets >= 44px, reflow 1-col, padding compacto.
    ============================================================ */
    @media (max-width: 640px) {
      main { padding: 16px 12px !important; }
      .mizu-section { padding: 16px 14px; border-left-width: 6px; }
      .sales-kpis,
      .sales-unit-grid,
      .meta-kpis-grid,
      .trends-grid,
      .ads-analysis-grid,
      .products-analysis-grid,
      .patterns-grid,
      .correlation-grid,
      .wr-grid,
      .roi-grid,
      .mkt-metrics,
      .mkt-paid {
        grid-template-columns: 1fr !important;
        gap: 10px;
      }
      .unit-breakdown-cards {
        grid-template-columns: 1fr !important;
      }
      .section-header {
        flex-direction: column;
        align-items: stretch;
      }
      .section-header-actions {
        margin-top: 8px;
      }
      .section-source-badge {
        align-self: flex-start;
      }
      /* Touch targets — botões e selects */
      button, select, input[type="text"], input[type="number"], input[type="date"], textarea {
        min-height: 44px;
      }
      .btn-sm, button.btn-sm {
        min-height: 38px;
      }
      /* Header — wrap nos filtros */
      .header-inner {
        flex-wrap: wrap;
        height: auto;
        padding: 12px 16px;
        gap: 10px;
      }
      .header-spacer { display: none; }
      .filter-sep { display: none; }
      .fg { width: 100%; }
      .fg select { width: 100%; }
      /* Hero summary fica menor em mobile */
      .hero-summary { font-size: 13px; padding: 10px 12px; }
      /* Kanji do section header fica menor */
      .section-header-icon { font-size: 18px; width: 32px; height: 32px; }
    }

    @media (max-width: 420px) {
      .uploads-area > summary { font-size: 12px; }
      .meta-kpi-val, .trend-card-value, .kpi-val, .mkt-tile-val { font-size: 22px; }
      .wr-roas-val { font-size: 24px; }
    }
    ```
  </action>
  <verify>npx tsc --noEmit && npm run build</verify>
  <done>build limpo</done>
</task>
</tasks>

<boundaries>
- Não mexer em .tsx
- Não regredir desktop layout (regras dentro de @media)
</boundaries>

<output>12-02-SUMMARY.md</output>
